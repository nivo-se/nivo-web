# Supabase Auth (deprecated)

**Auth is now Auth0 only.** See [AUTH_AUTH0_SETUP.md](./AUTH_AUTH0_SETUP.md) for setup.

Supabase is still used optionally for data/backend features (e.g. industry filters, some APIs). It is no longer used for authentication or JWT verification.
