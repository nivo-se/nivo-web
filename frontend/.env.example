# Frontend env — copy to .env (or set in Vercel Project → Settings → Environment Variables)
# Vite loads from frontend/.env or project root .env

# Backend API URL (production: tunnel URL)
VITE_API_BASE_URL=https://api.nivogroup.se
# Local dev: VITE_API_BASE_URL=http://127.0.0.1:8000

# Auth0 (same values in Vercel)
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id
VITE_AUTH0_AUDIENCE=https://api.nivogroup.se
